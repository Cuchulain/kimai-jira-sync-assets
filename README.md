# kimai-jira-sync-assets

Static landing page assets for Kimai Jira Sync.

## Local development

1. Install dependencies with pnpm.
2. Build the production stylesheet.
3. Open the HTML file in a browser.

### Commands

pnpm install
pnpm run build:css

For live CSS rebuilding while editing:

pnpm run watch:css
